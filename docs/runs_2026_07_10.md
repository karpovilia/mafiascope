# Batch runs 2026-07-10 (wave B1)

Both batches run with DeepSeek (`deepseek-chat`), snapshots enabled (`state.jsonl` in every game dir), chained probe battery, JSON-repair on.

Note: both batches were launched from the repo root, while the engine resolves the
log root as `../logs` relative to cwd (it assumes cwd=`src/`), so the logs were
first written to `~/repos/logs/` and moved into `logs/` afterwards. `results.json`
in the repo root is racy under concurrent batches — the ablation batch finished
last and overwrote the EN batch summary there; per-game outcomes below are taken
from each game's `game.jsonl`.

## EN demo corpus — `configs/config_en_demo.yaml` (P1-8)

5 games total: 36594b66 ran in the previous session; the other 4 in this batch
(wall-clock 2654 s for the batch of 4, parallel threads). Fixed `social_map`
wording (no demand-characteristic sentence). Mafia won 5/5.

| game | winner | rounds |
|---|---|---|
| 36594b66 | Mafia | 2 |
| 6a66aa14 | Mafia | 2 |
| e6fbee66 | Mafia | 2 |
| 0da78714 | Mafia | 3 |
| e8dfa6ea | Mafia | 3 |

## Ablation: old demand-characteristic `social_map` phrase — `configs/config_ablation_demand.yaml` (P1-3a / R1 W3 / DA C3a)

5 games, RU, exact copy of `config_deepseek.yaml` except the OLD `social_map`
wording. Compare probe answer distributions against the main RU corpus (new
wording) to quantify the demand-characteristic effect.

| game | winner | rounds |
|---|---|---|
| 0e2ba387 | Mafia | 2 |
| c7fe57de | Mafia | 3 |
| 797fd017 | Villagers | 3 |
| c9e66a2a | Villagers | 3 |
| f2510502 | Villagers | 15 |

## Replay pilot forks (wave B2, committed in 0d3db04)

`987ae95f` (POST fork of 36594b66 @ R1.7) and `0c6a5ddf` (PRE fork) — see
`replay_experiment.md`. Fork games are excluded from corpus metrics by
`forked_from` metadata.

## RU clean-wording batch — `configs/config_deepseek.yaml` post-fix (matched arm)

5 games, RU, same config as `ablation_demand` except the CLEAN `social_map`
wording — the matched arm the ablation lacked (main30 predates the wording
fix, so ablation-vs-main30 isolates the chaining fix, not the wording).
Launched from `src/` (logs land in `logs/` directly). Corpus `ru_clean`
in `metrics_lib`.

| game | winner | rounds |
|---|---|---|
| 444a8c24 | Mafia | 2 |
| 532d5f19 | Mafia | 2 |
| 4a48b59c | Mafia | 3 |
| 186742a9 | Mafia | 4 |
| 7b600523 | Villagers | 3 |

### Demand-phrase comparison (canonical F3 rule, repaired mode, boot 1000)

| corpus | wording | locale | games | pairs | acc | pred/actual suspects ratio |
|---|---|---|---|---|---|---|
| main30 | old | RU | 30 | 26664 | 44.8% | 1.63 CI [1.53, 1.74] |
| ablation_demand | old | RU | 5 | 7975 | 46.2% | 1.58 CI [1.46, 1.77] |
| **ru_clean** | **clean** | RU | 5 | 5076 | 52.3% | **1.48 CI [1.39, 1.61]** |
| en_demo | clean | EN | 5 | 3657 | 53.3% | 1.45 CI [1.18, 1.72] |

Reading: suspicion over-prediction persists with the clean wording
(1.48, CI well above 1.0) — the F3 spotlight effect is not an artifact of the
demand phrase. The phrase itself contributes a small increment
(1.58 vs 1.48, overlapping CIs — not separable at n=5 per arm).
